/**
 * jvf-dtm-types — sdílené doménové typy JVF DTM.
 *
 * Tento balíček obsahuje čisté TypeScript typy bez runtime kódu.
 * Konzumují ho `jvf-parser` (XML → typed objekty) a `jvf-topology`
 * (validace typovaných objektů).
 */
export * from './1.4.3/types.js';
export * from './versions.js';
