/**
 * Doménové typy JVF DTM 1.4.3.
 *
 * Autoritativní definice žije v balíčku `jvf-dtm-types` (sdíleno s `jvf-topology`).
 * Tento modul je jen re-export, aby se interní importy parseru
 * (`from '../types.js'`) nemusely přepisovat.
 */
export * from 'jvf-dtm-types';
