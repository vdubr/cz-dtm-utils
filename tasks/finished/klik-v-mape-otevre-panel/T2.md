# KMP-T2 — Info modal + changelog — ✅ hotovo

## Co se udělalo
- **`infoModal.ts`** (pravidlo CLAUDE.md): v popisu „Přehled prvků" doplněno,
  že klik na prvek v mapě (2D i 3D) je synchronní a **je-li panel zavřený,
  automaticky ho otevře** a prvek vybere (klik do prázdna nic nedělá).
- **`CHANGELOG.md`** `[Unreleased] › Změněno`: řádek o auto-otevření „Přehledu
  prvků" klikem na prvek v mapě (2D i 3D).

## Ověření (jak otestováno + výsledek)
- `npm run typecheck -w jvf-viewer` + `npm run build` zelené.
- e2e „modal O aplikaci se otevře a zavře" zelený.

## Dotčené soubory
- `jvf_viewer/src/ui/infoModal.ts`
- `CHANGELOG.md`
