# T4 — Parser: atributy + doprovodné info — ✅ hotovo

## Co se udělalo
- **Atributy** (skupina PSPI) hotové už v T3 (`src/1.5.0.1/attributes.ts`).
- **`src/1.5.0.1/doprovodne-informace.ts`** — plná implementace (nahradil seam
  z T3):
  - Projde všechny sourozence `DoprovodneInformace{,GAD,KAD,DTI,VydejZPS,
    VydejDTI,VydejPSPI}` v `DataJVFDTM`.
  - `OblastiKompletniZPS > ZaznamOKZPS[]` → `OblastKompletniZPSZaznam`:
    typ z **`TypZaznamuOKZPS`** (NEW/DEL/REF, 1.5.0.1 už ne z `PopisObjektu`),
    plocha z `PlochaZPS > surfaceProperty > Polygon` (defenzivně i přímo, R6),
    `UrovenUmisteniObjektuZPS` do attributes.
  - `UdajeOVydeji > TypDatoveSady` → `typDatoveSady`.
  - **KI se neparsuje** (zrušeno v 1.5.0.1, změna #8) — `parseOblastObjektuKI`
    ani `oblastObjektuKI` nejsou ve větvi 1.5.0.1.
- **`jvf_dtm_types`**: `OblastKompletniZPSTyp` += `'REF'` (aditivně, R1) —
  1.5.0.1 typ oblasti; topologie DEL-check reaguje jen na `'DEL'` → beze změny.

## Ověření (jak otestováno + výsledek)
- `tests/1.5.0.1/parser/doprovodne.test.ts` (5 testů): ZPS (OKZPS typ REF +
  plocha, TypDatoveSady=3), **PSPI export TypDatoveSady=11 = Výdej PSPI**,
  OPL (TypDatoveSady=2 + oblast), DI (bez výdeje → undefined), KI absence.
- `npm run typecheck` napříč všemi balíčky (vč. viewer) zelený.
- `npm test`: parser **4522** zelených, topology **214** zelených (aditivní
  změna sdíleného typu nic nerozbila).
- Build types + parser zelený; dist aktualizován.

## Další kroky (pokud jsou potřeba)
- Žádné pro T4. Router (T5) napojí `parseJvfDtm1501` do `parseJvfDtm(xml)`.

## Dotčené soubory
- `jvf_parser/src/1.5.0.1/doprovodne-informace.ts` (plná impl.)
- `jvf_dtm_types/src/1.4.3/types.ts` (`OblastKompletniZPSTyp` += `'REF'`)
- `jvf_parser/tests/1.5.0.1/parser/doprovodne.test.ts`
- `jvf_parser/dist/**`, `jvf_dtm_types/dist/**` (build)
