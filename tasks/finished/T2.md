# T2 — XSD/model/ukázky + codegen — ✅ hotovo

## Co se udělalo
- **Distribuce 1.5.0.1 uložena do repa** (ze 3 ZIPů z ČÚZK):
  - `jvf_parser/docs/1.5.0.1/xsd/` (+ `model/` YAML) — z `JVF_DTM_1501.aspx`.
  - `jvf_parser/samples/1.5.0.1/` — 8 ukázek (ZPS, DI, TI, OPL, PSPI import/
    export, protokol chyb, extenze) + `poznamka.txt` z `…_UkazkyXML.aspx`.
  - (Doplňkové PDF z `…_Doplnky.aspx` jen prohlédnuty pro referenci, do repa
    se nekopírují.)
- **Codegen parametrizován verzí** — `scripts/generate-types.ts [version]`
  (default `1.4.3`; npm `generate` = 1.4.3, `generate:1501` = 1.5.0.1):
  - 1.4.3 větev **beze změny** (R1 — regen 1.4.3 je bytově identický, ověřeno
    `git diff` po `git checkout`).
  - Nová 1.5.0.1 extrakce `extractEntity1501` (abstract `ZaznamObjektu` +
    substituční skupina → kanonický `Ins` complexType; geometrie mapovaná dle
    názvu typu `cmn:GeometrieObjektu…Type`; sdílené skupiny přes `name=`,
    specifické atr. přes `ref=`).
  - `SpolecneAtributyObjektuPSPI` doplněno do `SHARED_ATTR_GROUPS`.
  - `GEOM_TYPE_MAP_1501` (Bod/Linie/Plocha/PlochaZPS → GeomKind).
  - `extractSharedGroups1501` + `generateSharedAttrsFile1501` (pole z XSD
    kontextových variant; fix prefix-kolize `ZPS` vs `ZPS_TI`).
  - `obsahovaCast` union rozšířen o `'PSPI'`; PSPI objekty (code_base 0200000*)
    normalizovány na `obsahovaCast='PSPI'`.
- **Vygenerováno** `src/1.5.0.1/generated/{enums,shared-attrs,entities}.ts`
  (393 entit, 0 chyb, 136 enumů) + `src/1.5.0.1/types.ts` (re-export).
- **Integritní test (R8)** `tests/1.5.0.1/entities/catalog-integrity.test.ts`
  (7 testů) — nezávislé (regex) ověření katalogu proti XSD.

## Ověření (jak otestováno + výsledek)
- `npm run typecheck -w jvf-parser` — zelený (generované 1.5.0.1 kompilují).
- `npx vitest run` (jvf-parser) — **4507 testů zelených** (4500 stávajících
  1.4.3 + 7 nových integritních). 1.4.3 beze změny (R1).
- Akceptace: katalog obsahuje 21× `02000000XX` a nové `0100000372/381/388`;
  **neobsahuje `0100000185`**; PSPI mají `obsahovaCast=PSPI` +
  `SpolecneAtributyObjektuPSPI`. Vše ověřeno testem.
- `npm run build -w jvf-parser` — zelený; `dist/1.5.0.1/*` vygenerováno.

## Další kroky (pokud jsou potřeba)
- shared-attrs.ts 1.5.0.1 má pole jako sjednocení kontextů, všechna volitelná
  (type-level only; parser je data-driven). Případné zpřesnění optionality dle
  YAML `model/sady-atributu` je nice-to-have, ne blocker.
- T3 využije `ENTITY_CATALOG` + poznatky o geometrii (viz memory
  `jvf-1501-xsd-structure`).

## Dotčené soubory
- `jvf_parser/scripts/generate-types.ts` (parametrizace + 1.5.0.1 extrakce)
- `jvf_parser/package.json` (skript `generate:1501`)
- `jvf_parser/docs/1.5.0.1/**` (xsd + model)
- `jvf_parser/samples/1.5.0.1/**`
- `jvf_parser/src/1.5.0.1/types.ts` + `generated/{enums,shared-attrs,entities}.ts`
- `jvf_parser/tests/1.5.0.1/entities/catalog-integrity.test.ts`
- `jvf_parser/dist/1.5.0.1/**` (build)
