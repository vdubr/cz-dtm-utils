# T7 — Topologie 1.5.0.1 — ✅ hotovo

## Co se udělalo
- **`constants.ts`**: `DEFBOD_PLOCHA_PAIRS_1501` (66 párů, odvozeno z katalogu
  1.5.0.1 konvencí `{Kořen}DefinicniBod`+`{Kořen}Plocha`; oproti 1.4.3 přibyl
  mj. `RozestavenaPlocha` 0100000381; PSPI objekty nejsou v párech) +
  `OSA_OBVOD_PAIRS_1501` (PK, shodné s 1.4.3).
- **`relations.ts`**: `checkDefBodInPlocha`/`checkOsaInObvod` přijímají volitelný
  parametr `pairs` (default = 1.4.3 páry → zpětná kompatibilita, R1).
- **`index.ts`**:
  - `runAllChecks(dtm, mode | {mode, version})` — verzní; back-compat se
    stringovým `mode`.
  - `crossObjectChecksForVersion`: 1.5.0.1 → 1501 páry; prázdná/1.4.3 →
    default 1.4.3; **známá nepodporovaná verze → jen generické checky (R7)**.
  - `resolveMode`: `TypDatoveSady=11` (Výdej PSPI) → `complete`.
  - Export `DEFBOD_PLOCHA_PAIRS_1501`, `OSA_OBVOD_PAIRS_1501`.

## Ověření (jak otestováno + výsledek)
- `pairs-integrity.test.ts` rozšířen: 66 párů _1501 existuje v katalogu
  1.5.0.1, obsahuje RozestavenaPlocha, OSA/OBVOD platné, žádný pár není PSPI.
- `tests/1.5.0.1/topology/runall-1501.test.ts` (5 testů): 1501 pár
  RozestavenaPlocha → DEFBOD_OUTSIDE_PLOCHA; 1.4.3 tentýž pár neexistuje →
  bez chyby; neznámá verze 9.9.9 → meziobjektové přeskočeny (R7); Výdej PSPI
  (TypDatoveSady=11 + změnové věty) → complete → meziobjektové běží; bez něj
  changeset → přeskočeno.
- `npm test`: parser **4533** + topology **223** zelených (214 → 223,
  žádná regrese 1.4.3).
- `npm run typecheck` napříč balíčky zelený; topology build zelený.

## Další kroky (pokud jsou potřeba)
- Žádné. Viewer (T8) může volat `runAllChecks(dtm, { version })`.

## Dotčené soubory
- `jvf_topology/src/1.4.3/constants.ts` (páry _1501)
- `jvf_topology/src/1.4.3/relations.ts` (param pairs)
- `jvf_topology/src/1.4.3/index.ts` (runAllChecks verzní, resolveMode, exporty)
- `jvf_topology/tests/1.4.3/topology/pairs-integrity.test.ts` (rozšíření)
- `jvf_topology/tests/1.5.0.1/topology/runall-1501.test.ts` (nový)
- `jvf_topology/dist/**` (build)
