# T11 — Info modal + changelog + docs — ✅ hotovo

## Co se udělalo
- **`infoModal.ts`** (pravidlo CLAUDE.md — info modal musí odpovídat realitě):
  - „Verze JVF DTM" sekce přepsána — 2 podporované verze souběžně,
    **auto-detekce** verze + auto-přepnutí, ruční přepínač; přidán odstavec
    **Novinky 1.5.0.1** (PSPI, Výdej PSPI, operace v názvu elementu, zrušení
    KI / objektu).
  - Zastaralá věta „soubory musí odpovídat aktivní verzi" → auto-detekce.
  - Nový bod v „Hlavní funkce": **Protokol chyb** (ServisJVFDTM → report).
  - „Pro vývojáře": jvf-parser podporuje 1.4.3 i 1.5.0.1 + protokol chyb.
  - (`VERSIONS_DISPLAY` byl už dynamický z T1 → zobrazuje „1.4.3, 1.5.0.1".)
- **`CHANGELOG.md`** `[Unreleased]` — finalizován komplexní popis podpory
  1.5.0.1 napříč parser/topologie/viewer + PSPI (propaguje se do „Historie
  verzí" v info modalu přes `changelog.ts`).

## Ověření (jak otestováno + výsledek)
- `npm run typecheck -w jvf-viewer` + `npm run build` zelené (changelog se
  parsuje do info modalu při buildu — bez chyby).
- e2e „modal O aplikaci se otevře a zavře" zelený.

## Další kroky (pokud jsou potřeba)
- Žádné. Release (bump verzí, merge develop→main, tag, GitHub release) se dělá
  jen na explicitní pokyn — mimo scope běžce.

## Dotčené soubory
- `jvf_viewer/src/ui/infoModal.ts`
- `CHANGELOG.md`
