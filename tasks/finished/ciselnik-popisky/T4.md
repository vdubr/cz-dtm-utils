# CP-T4 — Info modal + changelog — ✅ hotovo

## Co se udělalo
- **`infoModal.ts`** (pravidlo CLAUDE.md): do popisu „Přehled prvků" doplněno,
  že u číselníkových atributů se vedle kódu zobrazí i český popisek z číselníku
  DTM (příklad `2 — dálnice II. třídy`).
- **`CHANGELOG.md`** `[Unreleased] › Přidáno`: nový řádek o popiskách
  číselníkových atributů (build-time `ENUM_LABELS` z XSD, jen sekce „Atributy
  objektu").

## Ověření (jak otestováno + výsledek)
- `npm run typecheck -w jvf-viewer` + `npm run build` zelené (changelog se
  propaguje do info modalu bez chyby).
- e2e „modal O aplikaci se otevře a zavře" zelený.

## Dotčené soubory
- `jvf_viewer/src/ui/infoModal.ts`
- `CHANGELOG.md`
